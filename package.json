{
  "name": "printer-bridge-monorepo",
  "private": true,
  "workspaces": [
    "bridge",
    "client",
    "react-lib",
    "example"
  ],
  "scripts": {
    "build:installer": "npm run build:installer -w bridge"
  }
}
